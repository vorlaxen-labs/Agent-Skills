# `completion`

Generate shell tab-completion scripts for bash, zsh, or fish.

```bash
agent-skills completion bash >> ~/.bashrc
agent-skills completion zsh  >> ~/.zshrc
agent-skills completion fish >> ~/.config/fish/completions/agent-skills.fish
```

Reload your shell or source the updated config file after installing.

Supported shells: `bash`, `zsh`, `fish`.
